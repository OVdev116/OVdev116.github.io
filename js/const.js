const items = document.querySelectorAll(".item");
const tabs = document.querySelectorAll(".link");
const nameTown = document.querySelectorAll(".name");
const temperatureTown = document.querySelectorAll(".temperature span");
const iconsMain = document.querySelector(".item-icon img");
const searchInput = document.querySelector(".search input");
const favoriteBtn = document.querySelector(".weather__favorites");
const locationList = document.querySelector(".location__list");
const close = document.querySelectorAll(".location__list .click");
const weatherList = document.querySelectorAll(".weather span");
const feelsList = document.querySelectorAll(".feels span");
const sunriseList = document.querySelector(".sunrise span");
const sunsetList = document.querySelector(".sunset span");
const wind = document.querySelector('.wind')
const humidity = document.querySelector('.humidity')
const count = document.querySelector(".count");
export {
    items,
    tabs,
    nameTown,
    temperatureTown,
    iconsMain,
    searchInput,
    favoriteBtn,
    locationList,
    close,
    weatherList,
    feelsList,
    sunriseList,
    sunsetList,
    wind,
    humidity,
    count
};
